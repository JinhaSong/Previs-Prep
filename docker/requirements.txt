# pc10k 포인트클라우드 추출·검증 (검증 환경: python3.10~3.11)
numpy<2
open3d==0.19.0
trimesh==4.8.2
scipy>=1.10
h5py
pillow
matplotlib
tqdm
# validate/functional_equiv.py (Uni3D 모델 동등성 검증, GPU) 사용 시 추가:
# torch==2.5.* torchvision timm ninja
# + pointnet2_ops (Pointnet2_PyTorch/pointnet2_ops_lib, TORCH_CUDA_ARCH_LIST 지정 컴파일)
